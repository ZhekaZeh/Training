---
platform: linux
image_resource:
    type: docker-image
    source:
        repository: bams-aws.refinitiv.com:5001/a250232/docker-builder
        tag: 1.0.30
        username: ((vault:kv/concourse-ci-ui.sa-csa-bams-username))
        password: ((vault:kv/concourse-ci-ui.sa-csa-bams-password))
outputs:
    - name: output-folder
run:
    path: sh
    user: root
    args:
        - -exc
        - |
            echo "${VERSION}{SUFFIX}" > output-folder/output
